package board;

// 1. Reply과 Article의 상카 Class(BaseClass)를 만든다.
public class BaseInfo {
    // 2. 두클래스의 공통변수를 뽑아낸다(공통기능은 없음. 데이터저장용 클래스라서)
    int id;
    String body;
    String regDate;
    int memberId;
    String nickname;
}
